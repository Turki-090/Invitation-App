# Database restore runbook

Restores must use a non-production target first. Record backup timestamp, recovery point, encryption/access controls, schema version, integrity checks, and application smoke-test results. A backup is considered proven only after a successful restore exercise.
